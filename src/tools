#!/home/xc_vm/bin/php/bin/php
<?php
// @legacy-proxy — redirect to console.php tools
// Logic moved to src/cli/Commands/ToolsCommand.php (rescue/access/ports subcommands)
$argv = array_merge(array('console.php', 'tools'), array_slice($argv, 1));
$argc = count($argv);
require __DIR__ . '/console.php';
